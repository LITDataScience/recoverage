[project]
name = "shop"
version = "0.0.1"
requires-python = ">=3.11"
description = "Shop library used by Recoverage's sample report."

dependencies = ["pytest>=8"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-p no:cacheprovider"
